[script]
n0=on admin:text:*:#:{
n1=  if ($1 == .unload) { .unload -rs $2- | msg $chan *** Descargado Script $2- }
n2=  if ($1 == .load)  { .load -rs $2- | msg $chan *** Cargado script $2- }
n3=  if ($1 == .akick) { .msg chan akick # add $2- }
